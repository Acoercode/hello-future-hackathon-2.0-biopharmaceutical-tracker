org: acoer
app: biopharma-tracker-functions
service: biopharma-tracker
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  timeout: 20

functions:
  api:
    handler: index.handler
    events:
      - httpApi: '*'

plugins:
  - serverless-offline
